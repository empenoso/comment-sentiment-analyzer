"""
📈 Парсер комментариев с финансового портала smart-lab.ru 📈

Этот Python-скрипт разработан для автоматического сбора комментариев
со статей на сайте Smart-Lab — популярном ресурсе среди трейдеров и
инвесторов. Он извлекает заголовок статьи, ее ID, а также детальную
информацию по каждому комментарию, включая автора, текст и дату.

Основные задачи:
- пакетная обработка: позволяет обрабатывать список URL-адресов статей
  за один запуск
- адаптация к сайту: автоматически добавляет параметр `?nomobile=1` к URL
  для загрузки десктопной версии сайта, что обеспечивает стабильность
  парсинга
- преобразование дат: содержит функцию `convert_smartlab_date` для
  конвертации специфичного для сайта формата даты (например, "28 сентября 2025, 08:12")
  в стандартный формат ISO 8601
- структурирование данных: собирает информацию о статье и комментариях
  в единый словарь для последующего сохранения
- сохранение в JSON: результаты парсинга для каждой статьи сохраняются
  в отдельный JSON-файл в папку `smart-lab_comments`, имя файла
  соответствует ID статьи
- надежность: включает обработку сетевых ошибок и ошибок парсинга,
  сообщая о проблемах в консоль

Порядок использования:
1. заполните список `urls_to_parse` ссылками на статьи Smart-Lab
2. запустите скрипт:
   python3 smart-lab_parser.py
3. json-файлы с результатами будут сохранены в директории `smart-lab_comments/`

Репозиторий проекта всегда доступен тут: https://github.com/empenoso/comment-sentiment-analyzer

Автор: Михаил Шардин https://shardin.name/
Дата создания: 04.10.2025
Версия: 1.0
"""
