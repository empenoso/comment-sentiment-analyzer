"""
🏦 Парсер комментариев для Тинькофф Журнала (t-j.ru) с использованием Playwright 🏦

Этот Python-скрипт предназначен для сбора комментариев со статей
Тинькофф Журнала, который использует динамическую загрузку контента
и защиту от ботов. Для обхода этих механизмов скрипт применяет
библиотеку Playwright, управляя реальным браузером.

Ключевые улучшения в версии 2.1:
- динамический поиск JSON: вместо привязки к статичному ID тега <script>,
  скрипт сначала извлекает уникальный articleUUID статьи, а затем ищет
  тег <script>, содержащий этот идентификатор. это делает парсер
  универсальным для любой статьи на сайте

Основные задачи:
- обход защиты от ботов: использует Playwright для запуска видимого
  браузера Chromium с настройками, маскирующими его под обычного
  пользователя (отключен флаг автоматизации, задан User-Agent)
- имитация поведения человека: применяет случайные задержки, прокрутку
  и нажатие на кнопку "Показать ещё", чтобы страница успела полностью
  загрузить все динамические компоненты, включая комментарии
- извлечение данных из JSON: находит на странице тег `<script>` с
  внедренными данными о комментариях в формате JSON и парсит их напрямую,
  что надежнее, чем анализ HTML-структуры
- пакетная обработка: способен обрабатывать список URL-адресов статей
  за один запуск, переиспользуя один экземпляр браузера для эффективности
- структурированное сохранение: сохраняет заголовок статьи, ее ID и
  список комментариев (ID, автор, дата, текст) в отдельный JSON-файл
  для каждой статьи в папку `t-j_comments`
- отказоустойчивость: обрабатывает ошибки времени ожидания и другие
  исключения, которые могут возникнуть при работе с Playwright

Порядок использования:
1. установите Playwright и его зависимости:
   pip install playwright beautifulsoup4
   python -m playwright install
2. добавьте целевые URL-адреса в список `urls_to_parse`
3. запустите скрипт:
   python3 t-j_parser.py
4. результаты будут сохранены в виде JSON-файлов в папке `t-j_comments/`

Репозиторий проекта всегда доступен тут: https://github.com/empenoso/comment-sentiment-analyzer

Автор: Михаил Шардин https://shardin.name/
Дата создания: 04.10.2025
Версия: 2.1
"""
